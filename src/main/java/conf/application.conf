# The main properties file
#
# Properties can be prefixed by "%".
# "%"" matches a mode you can set as system property. For instance when you
# are using maven you can do: mvn -Dmode=test jetty:run
#
# Using that the following property:
# %test.myproperty=test property
# would overwrite the property
# myproperty=my property
#
# You can define as many environments as you want. Simply set them in the mode.
application.name=Dog Park Meetups

application.cookie.prefix=KOIRAPUISTOT

#ISO Language Code, optionally followed by a valid ISO Country Code. 
application.languages=fi

application.session.expire_time_in_seconds=3600
application.session.send_only_if_changed=true
application.session.transferred_over_https_only=false


## -------------------------------------------------------------
## Ebean configuration:
## -------------------------------------------------------------
ebean.ddl.run=false

ebean.datasource.databaseUrl=jdbc:mysql://127.0.0.1:3306/dogparkmeetups
ebean.datasource.databaseDriver=com.mysql.jdbc.Driver
ebean.datasource.username=darwin
ebean.datasource.password=superd0g3

